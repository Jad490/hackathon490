# --- docker-compose.yml ---
version: "3.8"
services:
  web:
    build: .
    ports:
      - "8501:8501"
    environment:
      - PORT=8501
      - STREAMLIT_SERVER_HEADLESS=true
    # Uncomment the next two lines for live-reload during development
    # volumes:
    #   - .:/app
